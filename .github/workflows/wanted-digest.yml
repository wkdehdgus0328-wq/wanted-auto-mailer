- name: Run mailer (force test)
  env:
    SMTP_PASS: ${{ secrets.SMTP_PASS }}
    WM_CONFIG_PATH: config.json
    WM_STATE_PATH: last_ids.txt
    FORCE_TEST: "1"   # ✅ 신규 없어도 테스트 메일 발송
  run: python wanted_mailer.py
